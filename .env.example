# Server configuration for the next CitriFood phase.
# Copy to .env.local on your own machine or set these in the hosting provider.
# Do not commit actual values. Never expose the service role key in NEXT_PUBLIC_ variables.
SUPABASE_URL=
SUPABASE_ANON_KEY=
SUPABASE_SERVICE_ROLE_KEY=
